import { StyleSheet } from 'react-native';

export const s = StyleSheet.create({
    container: {
        flexDirection: 'row',
        alignItems: 'center',
        padding: 8,
        marginBottom: 14,
    },
    bold: {
        fontSize: 18,
        fontWeight: 'bold',
    },
    text: {
        lineHeight: 20,
        textAlign: 'justify'
    }
});